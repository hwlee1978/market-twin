{
  "name": "market-twin-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Run worker that runs ensemble simulations off Vercel's 800s function timeout. Receives orchestration requests from the web app, runs the full pipeline (persona / country / pricing / synthesis / market-profile / quality), and writes results back to Supabase.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && tsc-alias",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.1",
    "@google/generative-ai": "^0.24.1",
    "@hono/node-server": "^1.13.7",
    "@supabase/supabase-js": "^2.105.1",
    "hono": "^4.6.14",
    "openai": "^6.34.0",
    "resend": "^6.0.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsc-alias": "^1.8.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
