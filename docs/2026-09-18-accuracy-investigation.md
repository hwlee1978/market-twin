# 정확도 조사 리포트 — 2026-09-17 ~ 09-18

공개 정확도 수치를 갱신하려다 시작해서, **백테스트 자체의 결함 3건을 찾아 고치고, 엔진의 기여가 어디에 있는지를 처음으로 분리해 낸** 작업의 기록.

결론부터:

1. 공개 중인 top-2/top-3는 **브랜드를 전혀 모르는 기준선과 같은 수준**이다. 수치 자체는 정직하지만 엔진의 기여를 보여주지 못한다.
2. 엔진의 기여는 **"정답이 뻔하지 않은 건"에 몰려 있다.** 그 19건에서 1순위 42%·3순위 79%(무작위 11%·33%).
3. 시뮬레이션과 기준선이 **일치하는지 여부**가 이 조사에서 유일하게 두 번 재현된 신호다. 이걸 제품에 반영했다.
4. **홈페이지는 아직 바꾸지 않았다.** 로컬 커밋만 있고 푸시하지 않았다.

---

## 1. 고친 결함

### 1-1. as-of 날짜를 모델에게 알려준 적이 없었다 (`679dfc6`)

`projectInput.asOfDate`는 줄곧 존재했지만 읽는 곳은 `prefetch.ts`뿐이었다(Comtrade 기간, 관세 앵커 게이팅). `prompts.ts`에는 참조가 **0곳**. 모델은 2026년에 앉아 결과를 다 아는 채로, 시점에 맞는 무역 데이터만 첨부받아 답해 왔다. 그걸 "hindsight-controlled"라고 불러 왔다.

- `countryPrompt`·`personaPrompt`가 `asOfDate`가 있을 때만 as-of 블록을 렌더한다.
- 두 가지를 요구: ①그 시점 시장 조건으로 판단 ②브랜드의 후일 결과가 기억나도 근거가 아니며 역산 금지.
- 라이브 무영향 확인: 양쪽으로 렌더해 블록이 라이브 프롬프트에 없음을 검증(`scripts/_asof-render.ts`).

**측정 결과: 효과 없음.** 지시문을 넣어도 점수가 떨어지지 않았다(top-1 63%, 이전 61%). 모델은 자기 지식에 날짜표가 없어 무엇이 이후 정보인지 스스로 가르지 못한다. **"2016년인 척하라"로는 회상을 막을 수 없다는 것이 실측으로 확인됐다.**

### 1-2. 사전 웹 검색이 브랜드명 + 현재 연도로 검색하고 있었다 (`a93c745`)

관세·거버넌스·소셜버즈·GTM 탐색에는 as-of 가드가 있었는데 **Tavily 검색 3종에는 없었다**.

- 트렌드 쿼리: `food consumer trends 2025 2026 ... regional growth {브랜드명}`
- 한국어판: `... 해외 진출 수출 매출 ... {브랜드명}` / 일본어판: `... 海外展開 輸出 {브랜드명}`
- Shake Shack 실측: 결과 5건 전부 "2026 성장 전략"류 현재 기사.

2026-05-10 도입 이후 **모든 백테스트**(공개 수치 기준인 09-13 실행 포함)에 해당. 결과는 페르소나 프롬프트와 가격 단계로 들어갔다. KOL 검색은 브랜드명을 의도적으로 제외하고 있었으나 현재 연도를 쓰고 국가 순위 프롬프트로 직접 들어간다.

### 1-3. 페르소나 풀이 시대를 섞고 있었다 (`a93c745`)

풀은 (국가, 직업)으로만 키를 잡아 **언제 만들어진 페르소나인지 개념이 없다**. 2021년 픽스처용 페르소나가 1968년 픽스처에 재사용된다. as-of 실행 실측: **26,400 슬롯 중 32%가 풀 재사용** — 패널 3분의 1이 다른 시대였다.

두 결함 모두 `asOfDate`가 있으면 꺼진다. 검증: 같은 입력으로 as-of는 trend/margin/KOL = 0/0/0, 라이브는 9/5/5. 백테스트 로그에 `pool: 0 hits / 200 misses` 확인.

**수정 후 점수는 떨어지지 않았다**(61/76/84 → 61/76/84). 누출이 수치를 부풀리고 있던 것은 아니었다.

---

## 2. 회상(암기) 문제 — 이 코퍼스의 구조적 한계

### 측정한 것

| 실험 | 결과 |
|---|---|
| 이름만 주고 맞히기 (Sonnet 4.6, 3샘플) | **31/42 (74%)**, 41/42 만장일치 |
| 모델이 모르는 건에서 엔진 top-1 | 45~50% (블라인드 73~80%보다 **낮음**) |
| 익명화 15건 실행 | 엔진 top-1 53% → **40%** (블라인드와 동일) |
| 익명화해도 브랜드 식별됨 | **25/42가 3/3 식별** |

### 핵심 발견

브랜드명을 지우고 특징적 디테일까지 3라운드에 걸쳐 일반화해도 **42건 중 27건은 모델이 브랜드를 맞힌다.** "뉴질랜드의 클라우드 회계 플랫폼" → Xero, "미국의 스트리밍 구독" → Netflix. 카테고리+원산지+포지셔닝만으로 식별된다.

원인은 익명화 실패가 아니라 **코퍼스 선정 방식**이다. 백테스트 픽스처는 결과가 문서화된 브랜드여야 하고, 문서화된 성공 사례는 모델도 학습했다. **유명할수록 정답이 확실하고, 유명할수록 모델이 안다.**

익명화 15건(식별 불가 건만)에서 top-1은 53%→40%로 떨어졌지만 **top-3는 73%→80%로 떨어지지 않았고 평균 순위도 2.40으로 동일**했다. 회상은 1위 선택에 몰려 있고, 후보군 품질은 브랜드 정체 없이도 유지된다.

---

## 3. 최종 측정 (누출 수정 후, 42건)

**top-1 60% · top-2 76% · top-3 83%** ($93.51, 2시간 41분, 42/42 완주)

### 블라인드 기준선과 비교 (N=39)

| | top-1 | top-2 | top-3 | 평균순위 |
|---|---|---|---|---|
| 엔진(수정 후) | 61% | **76%** | **84%** | 1.97 |
| 블라인드 전체순위 | 51% | **76%** | **84%** | 2.18 |

top-2·top-3가 동일. 엔진이 앞서는 건 top-1(+8pp, 8승5패, p=0.58)과 평균순위뿐이고 미판정.

### 난이도로 나누면 (블라인드가 1위를 틀린 건 = 어려움)

| | 엔진 top-1 | top-2 | top-3 |
|---|---|---|---|
| **어려운 19건** | **42%** | 63% | **79%** |
| 무작위 기대 | 11% | 22% | 33% |
| 쉬운 20건 | 75% | 90% | 90% |

쉬운 20건에서는 오히려 5건을 놓쳤다(CHAGEE·Krating Daeng·Mixue·Netflix·Paris Baguette). **전체 합계가 기준선과 같아지는 이유가 이것이다.**

---

## 4. 제품 변경 (`51de3ac`)

### 결합 방식 검증

| 규칙 | 적중 | 평균 추천 수 |
|---|---|---|
| 고정 엔진 top-2 (기존) | 77% | 2.00 |
| **적응형: 일치→1곳 / 불일치→3곳** | **82%** | **1.90** |
| 목록 병합(엔진1위+블라인드1위) | 72% | 2.00 |

**목록을 합치는 건 나쁘다** — 블라인드 1위가 대개 엔진 top-2 안에 이미 있어 칸을 낭비한다.

### 일치 신호 (두 번 재현)

| 실행 | 일치 시 적중 | 불일치 시 | p |
|---|---|---|---|
| run G | 79% (15/19) | 33% (7/21) | 0.0051 |
| 수정 실행 | 79% (15/19) | 40% (8/20) | 0.0225 |

불일치일 때 블라인드도 25%로 낮다 — **"다른 쪽이 맞다"가 아니라 "둘 다 못 믿는다"**는 뜻.

### 적용한 것

- 교차검증 **기본값 ON** (`SIM_BLIND_CROSSCHECK=off`로 끔)
- 일치 → 1곳 추천 / 불일치 → 3곳(엔진 top-2 + 기준선 픽) + `shortlist` 필드
- 결과 화면에 교차검증 배너 (일치=초록 / 불일치=주황)
- 기존 앙상블은 `crossCheck` 필드가 없어 이전과 동일하게 표시

---

## 5. 계측 복구 (미커밋)

`llm_usage_log`는 **2026-05-29 이후 기록이 없었다.** 원인: 시뮬레이션 파이프라인이 `usageContext`를 한 번도 넘기지 않아, API 라우트가 주변 컨텍스트를 걸어준 경우에만 기록됐다. 백그라운드·CLI 실행은 전부 누락.

- `runner.ts`: 프로바이더 생성 전에 워크스페이스를 조회해 4개 단계 전부에 `usageContext` 부착
- `smoke-ensemble-e2e.ts`: CLI에서도 로거가 등록되도록 side-effect import (Next.js `instrumentation.ts`는 스크립트에서 안 돈다)
- 가격 단계 토큰 로그 추가 — 유일하게 계측이 없던 단계

**가격 단계 실측:** 5샘플 기준 입력 18,360 / 출력 2,257 토큰 → 시뮬당 $0.023, **시장 1개 추가당 약 $0.07**(hypothesis 3시뮬 기준). 3곳 추천이면 앙상블당 +$0.14, 원가의 약 5%.

---

## 6. 비용

| 항목 | 금액 |
|---|---|
| as-of 지시문 검증 실행 (42건) | $101.00 |
| 익명화 실행 (15건) | $35.19 |
| 누출 수정 후 재실행 (42건) | $93.51 |
| 가격 계측 (1건) | ~$2.40 |
| 프로브류(이름만·블라인드·익명화 생성) | ~$5 |
| **합계** | **약 $237** |

---

## 7. 홈페이지 상태

**아직 반영하지 않았다.** 별도 저장소(`markettwin-web`)에 로컬 커밋 2개(`841e8c7`, `4a6b083`)가 있고 **푸시하지 않았다.** 공개 페이지는 여전히 Top-2 68% / Top-3 74%.

`proposals/`의 IR 문서는 hindsight 문구를 "시점 통제(부분)"로 정정했으나 **요청에 따라 커밋하지 않았다**(작업 트리에만 존재).

---

## 8. 미해결

- **공개 수치를 어떻게 쓸지 미정.** 76/83으로 올릴지, 68/74를 유지할지, 난이도 프레이밍으로 갈지.
- **적응형 규칙은 사후 선택.** 82% vs 77%는 2건 차이(2승 0패, p=0.50). 다음 코퍼스에서 **규칙을 먼저 동결하고** 재검증해야 한다.
- **블라인드 기준선이 형식에 민감하다.** 같은 15건에서 단일픽 40% vs 순위 27%. 기준선 수치를 단정적으로 인용하면 안 된다.
- **블라인드 순위 3건 생성 실패**(Din Tai Fung·Cath Kidston·Five Guys) — 분모가 42가 아니라 39인 이유. 원인 미조사.
- **0xC0000409 네이티브 크래시** — 실행당 5% 내외, 재시도로 회수 중. 원인 미규명.

## 9. 다음 단계

1. **전향적 코호트 첫 확인** — 2026-10. 15건 예측이 2026-07-06에 동결돼 있고 DB와 문서가 정확히 일치함을 확인했다. 회상 오염이 원천적으로 없는 유일한 근거.
2. **무명 중소기업 코퍼스** — KOTRA 기업성공사례 API(275건, 무료, 키 보유)에서 소비재 96건이 후보. 기업별 국가별 수출실적은 **관세법 116조로 공개 조회 불가**하므로 정답은 다중 증거로 확정해야 한다.
3. **적응형 규칙 사전등록 후 재검증.**
